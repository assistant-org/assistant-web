/** Printable A4 width at 96dpi. */
export const CONTRACT_PAGE_WIDTH_PX = 794;

/** Inner HTML padding (~20mm sides, ~25mm top/bottom). */
export const CONTRACT_CONTENT_PADDING = {
  top: 76,
  right: 68,
  bottom: 76,
  left: 68,
} as const;

/** White border on exported PDF pages. */
export const CONTRACT_PDF_MARGIN_MM = 15;
